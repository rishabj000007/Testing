// math.js - This is our function that adds two numbers
function add(a, b) {
  return a + b;  // Simply adds a + b and returns the result
}

export default add;  // This lets other files use this function
